{
  "name": "vanilla",
  "version": "1.0.0",
  "main": "index.html",
  "scripts": {
    "dev": "bun --watch run server.js",
    "lint": "oxlint",
    "reinstall": "rm -rf node_modules bun.lock && bun install"
  },
  "dependencies": {
    "hono": "^4.10.3"
  },
  "devDependencies": {
    "oxlint": "^1.24.0",
    "vite": "^7.1.12"
  }
}
